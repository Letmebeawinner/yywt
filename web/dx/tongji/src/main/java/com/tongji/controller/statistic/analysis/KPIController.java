package com.tongji.controller.statistic.analysis;

import com.a_268.base.controller.BaseController;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

/**
 * KPI(关键绩效指标)统计分析Controller
 *
 * @author sk
 * @since 2017-02-28
 */
@Controller
@RequestMapping("/admin/statistic/analysis/kpi")
public class KPIController extends BaseController {

    private static final Logger logger = LoggerFactory.getLogger(KPIController.class);

    /**
     * 删除KPI
     */
    private void deleteKPI() {
    }


    /**
     * 添加KPI
     */
    private void insertKPI() {
    }

    /**
     * 更新KPI
     */
    private void updateKPI() {
    }
}
